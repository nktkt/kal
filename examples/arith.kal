# 基本の電卓: 演算子の優先順位を確認する
# それぞれのトップレベル式の値が自動で表示される

1 + 2 * 3;        # => 7  (* が + より強い)
(1 + 2) * 3;      # => 9
10 / 4;           # => 2.5
2 > 1;            # => 1  (真は 1.0)
1 > 2;            # => 0  (偽は 0.0)
