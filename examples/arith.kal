# 基本の電卓: 整数の既定は i32、比較結果は bool、除算は型で挙動が変わる

1 + 2 * 3;       # => 7   (* が + より強い)
(1 + 2) * 3;     # => 9
2 > 1;           # => 1   (bool: 真)
1 > 2;           # => 0   (bool: 偽)
7 / 2;           # => 3   (整数除算)
7.0 / 2.0;       # => 3.5 (浮動小数点)
3.14 * 2.0;      # => 6.28
