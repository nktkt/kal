# 明示キャスト: 暗黙の数値変換はないので `as` で変換する

fn half(n: i64) -> f64 = (n as f64) / 2.0;

half(7);        # => 3.5
9 as f64;       # => 9    (i32 → f64)
3.9 as i64;     # => 3    (f64 → i64, 切り捨て)
255 as u8;      # => 255
